{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agenticwork.io/schemas/skill-manifest.schema.json",
  "title": "SkillManifestSchema",
  "description": "JSON Schema định nghĩa cấu trúc chuẩn của YAML Frontmatter trong mỗi file SKILL.md (Intent Contract)",
  "type": "object",
  "required": ["name", "description", "intentCategory", "triggers", "requires"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Tên định danh duy nhất của Skill (viết thường, dùng dấu gạch ngang)"
    },
    "description": {
      "type": "string",
      "description": "Mô tả chi tiết mục tiêu, ứng dụng và hoàn cảnh dùng Skill"
    },
    "intentCategory": {
      "type": "string",
      "enum": [
        "CODE_GEN",
        "REQUIREMENTS_REFINEMENT",
        "BUG_FIX",
        "ARCHITECTURE_DESIGN",
        "DOCUMENTATION",
        "TESTING",
        "DATABASE_MIGRATION",
        "UI_COMPONENT"
      ],
      "description": "Phân loại nhóm ý định nghiệp vụ của Skill"
    },
    "triggers": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string"
      },
      "description": "Danh sách các từ khóa hoặc cụm câu lệnh (VI/EN) kích hoạt Skill"
    },
    "requires": {
      "type": "object",
      "description": "Khai báo tường minh các tài nguyên bối cảnh cần Knowledge Agent nạp",
      "properties": {
        "templates": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Danh sách các đường dẫn template file trong docs/00-Meta/Templates"
        },
        "registries": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Danh sách các registry file (00-INDEX.md, module-registry.md)"
        },
        "astGraphSymbols": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Danh sách từ khóa AST Code Graph cần bóc tách"
        },
        "npmPackages": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Các gói thư viện phụ thuộc"
        }
      }
    },
    "version": {
      "type": "string",
      "default": "1.0.0"
    },
    "author": {
      "type": "string"
    },
    "is3rdParty": {
      "type": "boolean",
      "default": false
    },
    "modelPreference": {
      "type": "string",
      "enum": ["HIGH_PERFORMANCE", "FAST_ROUTING"],
      "default": "HIGH_PERFORMANCE"
    }
  },
  "additionalProperties": false
}
